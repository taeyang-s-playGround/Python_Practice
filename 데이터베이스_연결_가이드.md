# Django 데이터베이스 연결 완전 가이드

## 📋 목차
1. [Django의 데이터베이스 설정](#1-django의-데이터베이스-설정)
2. [SQLite 연결 (기본 설정)](#2-sqlite-연결-기본-설정)
3. [PostgreSQL 연결하기](#3-postgresql-연결하기)
4. [MySQL 연결하기](#4-mysql-연결하기)
5. [데이터베이스 연결 확인](#5-데이터베이스-연결-확인)
6. [환경 변수로 설정 관리 (보안)](#6-환경-변수로-설정-관리-보안)
7. [데이터베이스 관리 도구](#7-데이터베이스-관리-도구)
8. [트러블슈팅](#8-트러블슈팅)

---

## 1. Django의 데이터베이스 설정

Django는 `settings.py`에서 데이터베이스 연결을 설정합니다.

**위치**: `mysite/mysite/settings.py`

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

### 코드 분석

**`'default'`**: 기본 데이터베이스 연결 이름
- 여러 데이터베이스를 사용할 수 있지만, 기본은 `default`
- 다른 이름으로도 설정 가능 (예: `'read'`, `'write'`)

**`'ENGINE'`**: 사용할 데이터베이스 엔진
- `'django.db.backends.sqlite3'`: SQLite 사용
- `'django.db.backends.postgresql'`: PostgreSQL 사용
- `'django.db.backends.mysql'`: MySQL 사용
- `'django.db.backends.oracle'`: Oracle 사용

**`'NAME'`**: 데이터베이스 파일 경로 또는 이름
- SQLite: 파일 경로 (`BASE_DIR / 'db.sqlite3'`)
- PostgreSQL/MySQL: 데이터베이스 이름 (`'myproject_db'`)

**기타 설정 항목들:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'myproject_db',        # 데이터베이스 이름
        'USER': 'myuser',              # 사용자 이름
        'PASSWORD': 'mypassword',       # 비밀번호
        'HOST': 'localhost',           # 호스트 주소
        'PORT': '5432',                # 포트 번호
        'OPTIONS': {                   # 추가 옵션
            'charset': 'utf8mb4',
        },
        'CONN_MAX_AGE': 600,           # 연결 유지 시간 (초)
    }
}
```

---

## 2. SQLite 연결 (기본 설정)

### SQLite란?
- 파일 기반 데이터베이스
- 별도 설치 없이 사용 가능
- 개발 및 소규모 프로젝트에 적합
- Django의 기본 데이터베이스

### 장점
- ✅ 설정이 간단함
- ✅ 별도의 서버 설치 불필요
- ✅ 파일 하나로 모든 데이터 관리
- ✅ 빠른 개발 및 테스트에 적합
- ✅ Python 표준 라이브러리에 포함

### 단점
- ❌ 동시 접근에 제한적 (읽기는 가능, 쓰기는 제한)
- ❌ 대규모 프로젝트에는 부적합
- ❌ 네트워크 접근 불가 (로컬 파일만)
- ❌ 복잡한 쿼리 성능 제한

### 사용 방법

**설정은 이미 완료되어 있음:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

**데이터베이스 파일 위치:**
```
mysite/
└── db.sqlite3  # 여기에 자동 생성됨
```

**마이그레이션 실행 시 자동 생성:**
```bash
cd mysite
python manage.py migrate
```

**결과:**
- `db.sqlite3` 파일이 없으면 자동으로 생성됨
- 모든 테이블이 생성됨

**데이터베이스 파일 확인:**
```bash
# 파일이 생성되었는지 확인
ls -lh mysite/db.sqlite3

# 파일 크기 확인
du -h mysite/db.sqlite3
```

### SQLite 파일 백업

```bash
# 백업
cp db.sqlite3 db.sqlite3.backup

# 복원
cp db.sqlite3.backup db.sqlite3
```

---

## 3. PostgreSQL 연결하기

### PostgreSQL이란?
- 강력한 오픈소스 관계형 데이터베이스
- 프로덕션 환경에서 많이 사용
- ACID 트랜잭션 지원
- 복잡한 쿼리와 대용량 데이터 처리에 우수

### 설치

#### macOS

**1. Homebrew로 설치:**
```bash
# PostgreSQL 설치
brew install postgresql@14

# 서비스 시작
brew services start postgresql@14

# 버전 확인
psql --version
```

**2. PostgreSQL 접속:**
```bash
# 기본 사용자로 접속
psql postgres

# 또는 특정 사용자로
psql -U postgres
```

#### Ubuntu/Debian

**1. 패키지 업데이트:**
```bash
sudo apt-get update
```

**2. PostgreSQL 설치:**
```bash
sudo apt-get install postgresql postgresql-contrib
```

**3. 서비스 시작:**
```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql  # 부팅 시 자동 시작
```

**4. PostgreSQL 접속:**
```bash
sudo -u postgres psql
```

#### Windows

**1. 다운로드:**
- https://www.postgresql.org/download/windows/ 에서 설치 프로그램 다운로드

**2. 설치:**
- 설치 마법사를 따라 진행
- 포트 번호: 5432 (기본값)
- 비밀번호 설정 (기억해두기!)

**3. 접속:**
- pgAdmin 또는 명령 프롬프트에서 `psql` 사용

### 데이터베이스 생성

**1. PostgreSQL 접속:**
```bash
psql -U postgres
```

**2. 데이터베이스 생성:**
```sql
-- 데이터베이스 생성
CREATE DATABASE myproject_db;

-- 인코딩 설정 (한글 지원)
CREATE DATABASE myproject_db 
    WITH ENCODING 'UTF8' 
    LC_COLLATE='ko_KR.UTF-8' 
    LC_CTYPE='ko_KR.UTF-8';
```

**3. 사용자 생성 (선택사항):**
```sql
-- 사용자 생성
CREATE USER myuser WITH PASSWORD 'mypassword';

-- 권한 부여
GRANT ALL PRIVILEGES ON DATABASE myproject_db TO myuser;

-- 사용자 확인
\du
```

**4. 데이터베이스 목록 확인:**
```sql
\l
```

**5. 데이터베이스 접속:**
```sql
\c myproject_db
```

**6. PostgreSQL 종료:**
```sql
\q
```

### Django 설정

**1. 패키지 설치:**
```bash
pip install psycopg2-binary
```

**설명:**
- `psycopg2`: PostgreSQL 어댑터
- `psycopg2-binary`: 바이너리 버전 (설치가 쉬움)
- 프로덕션에서는 `psycopg2` 사용 권장

**2. settings.py 수정:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'myproject_db',
        'USER': 'myuser',
        'PASSWORD': 'mypassword',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

**설정 항목 설명:**
- `ENGINE`: PostgreSQL 엔진 사용
- `NAME`: 데이터베이스 이름 (위에서 생성한 이름)
- `USER`: 데이터베이스 사용자 이름
- `PASSWORD`: 비밀번호
- `HOST`: 데이터베이스 서버 주소
  - 로컬: `'localhost'` 또는 `'127.0.0.1'`
  - 원격: 서버 IP 주소 또는 도메인
- `PORT`: 포트 번호 (기본값 5432)

**3. 마이그레이션 적용:**
```bash
python manage.py migrate
```

**4. 연결 확인:**
```bash
python manage.py dbshell
```

PostgreSQL 쉘이 열리면 연결 성공!

---

## 4. MySQL 연결하기

### MySQL이란?
- 널리 사용되는 오픈소스 관계형 데이터베이스
- 웹 애플리케이션에서 많이 사용
- 빠른 읽기 성능

### 설치

#### macOS

**1. Homebrew로 설치:**
```bash
# MySQL 설치
brew install mysql

# 서비스 시작
brew services start mysql

# 보안 설정
mysql_secure_installation
```

**2. MySQL 접속:**
```bash
mysql -u root -p
```

#### Ubuntu/Debian

**1. 패키지 업데이트:**
```bash
sudo apt-get update
```

**2. MySQL 설치:**
```bash
sudo apt-get install mysql-server
```

**3. 보안 설정:**
```bash
sudo mysql_secure_installation
```

**4. 서비스 시작:**
```bash
sudo systemctl start mysql
sudo systemctl enable mysql
```

**5. MySQL 접속:**
```bash
sudo mysql -u root -p
```

#### Windows

**1. 다운로드:**
- https://dev.mysql.com/downloads/installer/ 에서 설치 프로그램 다운로드

**2. 설치:**
- 설치 마법사를 따라 진행
- 포트 번호: 3306 (기본값)
- root 비밀번호 설정

### 데이터베이스 생성

**1. MySQL 접속:**
```bash
mysql -u root -p
```

**2. 데이터베이스 생성:**
```sql
-- 데이터베이스 생성 (UTF-8 인코딩)
CREATE DATABASE myproject_db 
    CHARACTER SET utf8mb4 
    COLLATE utf8mb4_unicode_ci;

-- 데이터베이스 목록 확인
SHOW DATABASES;

-- 데이터베이스 선택
USE myproject_db;
```

**3. 사용자 생성 (선택사항):**
```sql
-- 사용자 생성
CREATE USER 'myuser'@'localhost' IDENTIFIED BY 'mypassword';

-- 권한 부여
GRANT ALL PRIVILEGES ON myproject_db.* TO 'myuser'@'localhost';

-- 권한 적용
FLUSH PRIVILEGES;

-- 사용자 확인
SELECT user, host FROM mysql.user;
```

### Django 설정

**1. 패키지 설치:**
```bash
# macOS
brew install mysql-client
pip install mysqlclient

# Ubuntu/Debian
sudo apt-get install python3-dev default-libmysqlclient-dev build-essential
pip install mysqlclient

# Windows
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#mysqlclient 에서 .whl 파일 다운로드
pip install mysqlclient‑1.4.6‑cp39‑cp39‑win_amd64.whl
```

**2. settings.py 수정:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'myproject_db',
        'USER': 'root',
        'PASSWORD': 'yourpassword',
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
        },
    }
}
```

**설정 항목 설명:**
- `ENGINE`: MySQL 엔진 사용
- `NAME`: 데이터베이스 이름
- `USER`: 사용자 이름 (기본: `root`)
- `PASSWORD`: 비밀번호
- `HOST`: 호스트 주소 (로컬: `'localhost'`)
- `PORT`: 포트 번호 (기본값 3306)
- `OPTIONS`: 추가 옵션 (UTF-8 인코딩 설정)

**3. 마이그레이션 적용:**
```bash
python manage.py migrate
```

**4. 연결 확인:**
```bash
python manage.py dbshell
```

---

## 5. 데이터베이스 연결 확인

### 방법 1: Django 쉘 사용

**Django 쉘 실행:**
```bash
python manage.py shell
```

**연결 확인 코드:**
```python
# 데이터베이스 연결 확인
from django.db import connection

# 연결 확인
connection.ensure_connection()
# None이 반환되면 성공

# 연결 정보 확인
print(connection.settings_dict)

# 모델 사용 테스트
from posts.models import Post, Comment

# 쿼리 실행 (연결 성공 시 데이터 반환)
posts = Post.objects.all()
print(posts)

# 댓글 조회
comments = Comment.objects.all()
print(comments)
```

### 방법 2: 마이그레이션 실행

**마이그레이션 실행:**
```bash
python manage.py migrate
```

**성공 시:**
```
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, posts, sessions
Running migrations:
  Applying posts.0001_initial... OK
  Applying posts.0002_comment... OK
```

**실패 시:**
```
django.db.utils.OperationalError: (2002, "Can't connect to MySQL server...")
```

에러 메시지를 확인하여 설정을 수정하세요.

### 방법 3: 서버 실행

**서버 실행:**
```bash
python manage.py runserver
```

**성공 시:**
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CONTROL-C.
```

**실패 시:**
데이터베이스 관련 에러가 콘솔에 표시됩니다.

### 방법 4: dbshell 사용

**데이터베이스 쉘 실행:**
```bash
python manage.py dbshell
```

**SQLite:**
```sql
sqlite> .tables
sqlite> SELECT * FROM posts_post;
sqlite> .quit
```

**PostgreSQL:**
```sql
myproject_db=# \dt
myproject_db=# SELECT * FROM posts_post;
myproject_db=# \q
```

**MySQL:**
```sql
mysql> SHOW TABLES;
mysql> SELECT * FROM posts_post;
mysql> exit;
```

---

## 6. 환경 변수로 설정 관리 (보안)

### 왜 환경 변수를 사용하나요?

**문제점:**
```python
# ❌ 나쁜 예: 비밀번호가 코드에 노출됨
DATABASES = {
    'default': {
        'PASSWORD': 'mypassword123',  # Git에 커밋되면 위험!
    }
}
```

**해결책:**
- 비밀번호 등 민감한 정보를 코드에 직접 작성하지 않음
- 설정을 환경별로 다르게 관리 가능
- Git에 커밋해도 안전

### 사용 방법

#### 방법 1: python-decouple 사용 (권장)

**1. 패키지 설치:**
```bash
pip install python-decouple
```

**2. .env 파일 생성:**
프로젝트 루트에 `.env` 파일 생성:
```
# .env 파일
DB_ENGINE=django.db.backends.postgresql
DB_NAME=myproject_db
DB_USER=myuser
DB_PASSWORD=mypassword
DB_HOST=localhost
DB_PORT=5432
```

**3. settings.py 수정:**
```python
from decouple import config

DATABASES = {
    'default': {
        'ENGINE': config('DB_ENGINE', default='django.db.backends.sqlite3'),
        'NAME': config('DB_NAME', default=BASE_DIR / 'db.sqlite3'),
        'USER': config('DB_USER', default=''),
        'PASSWORD': config('DB_PASSWORD', default=''),
        'HOST': config('DB_HOST', default='localhost'),
        'PORT': config('DB_PORT', default=''),
    }
}
```

**설명:**
- `config('KEY', default='기본값')`: 환경 변수에서 값 가져오기
- 환경 변수가 없으면 기본값 사용
- 개발 환경에서는 `.env` 파일 사용
- 프로덕션에서는 실제 환경 변수 사용

**4. .gitignore에 추가:**
```
# .gitignore
.env
*.env
```

**.env 파일은 절대 Git에 커밋하지 마세요!**

#### 방법 2: os.environ 사용

**settings.py:**
```python
import os

DATABASES = {
    'default': {
        'ENGINE': os.environ.get('DB_ENGINE', 'django.db.backends.sqlite3'),
        'NAME': os.environ.get('DB_NAME', BASE_DIR / 'db.sqlite3'),
        'USER': os.environ.get('DB_USER', ''),
        'PASSWORD': os.environ.get('DB_PASSWORD', ''),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get('DB_PORT', ''),
    }
}
```

**환경 변수 설정:**
```bash
# macOS/Linux
export DB_PASSWORD=mypassword

# Windows
set DB_PASSWORD=mypassword
```

#### 방법 3: django-environ 사용

**1. 패키지 설치:**
```bash
pip install django-environ
```

**2. settings.py:**
```python
import environ

env = environ.Env(
    DEBUG=(bool, False)
)

environ.Env.read_env()  # .env 파일 읽기

DATABASES = {
    'default': env.db(),  # DATABASE_URL 환경 변수 사용
}
```

**.env 파일:**
```
DATABASE_URL=postgres://myuser:mypassword@localhost:5432/myproject_db
```

### 프로덕션 환경 설정

**환경 변수 설정:**
```bash
# Linux/macOS
export DB_PASSWORD=production_password

# 또는 .env 파일 사용 (서버에 직접 생성)
```

**Docker 사용 시:**
```yaml
# docker-compose.yml
services:
  web:
    environment:
      - DB_PASSWORD=${DB_PASSWORD}
```

---

## 7. 데이터베이스 관리 도구

### SQLite Browser

**기능:**
- SQLite 파일을 시각적으로 확인
- 테이블 구조 및 데이터 확인
- SQL 쿼리 실행

**다운로드:**
- https://sqlitebrowser.org/

**사용 방법:**
1. DB Browser for SQLite 실행
2. `db.sqlite3` 파일 열기
3. Browse Data 탭에서 데이터 확인
4. Execute SQL 탭에서 쿼리 실행

### pgAdmin (PostgreSQL)

**기능:**
- PostgreSQL 관리 도구
- 그래픽 인터페이스
- 쿼리 실행 및 데이터 확인

**다운로드:**
- https://www.pgadmin.org/

**사용 방법:**
1. pgAdmin 실행
2. 서버 연결 설정
3. 데이터베이스 선택
4. 테이블 및 데이터 확인

### phpMyAdmin (MySQL)

**기능:**
- MySQL 관리 도구
- 웹 기반 인터페이스
- 브라우저에서 접근 가능

**설치:**
```bash
# Ubuntu
sudo apt-get install phpmyadmin

# 접속
http://localhost/phpmyadmin
```

### Django Admin

**기능:**
- Django 내장 관리 도구
- 모델 데이터 관리
- 사용자 친화적 인터페이스

**사용 방법:**
1. 슈퍼유저 생성:
```bash
python manage.py createsuperuser
```

2. 서버 실행:
```bash
python manage.py runserver
```

3. 접속:
```
http://localhost:8000/admin/
```

4. 모델 등록 (`admin.py`):
```python
from django.contrib import admin
from .models import Post, Comment

admin.site.register(Post)
admin.site.register(Comment)
```

### DBeaver (범용)

**기능:**
- 여러 데이터베이스 지원 (SQLite, PostgreSQL, MySQL 등)
- 무료 및 오픈소스
- 강력한 쿼리 편집기

**다운로드:**
- https://dbeaver.io/

---

## 8. 트러블슈팅

### 문제 1: 연결 거부됨

**에러:**
```
django.db.utils.OperationalError: (2002, "Can't connect to MySQL server...")
```

**해결 방법:**
1. 데이터베이스 서버가 실행 중인지 확인
```bash
# PostgreSQL
brew services list  # macOS
sudo systemctl status postgresql  # Linux

# MySQL
brew services list  # macOS
sudo systemctl status mysql  # Linux
```

2. 서버 시작
```bash
# PostgreSQL
brew services start postgresql  # macOS
sudo systemctl start postgresql  # Linux

# MySQL
brew services start mysql  # macOS
sudo systemctl start mysql  # Linux
```

### 문제 2: 인증 실패

**에러:**
```
django.db.utils.OperationalError: FATAL: password authentication failed
```

**해결 방법:**
1. 사용자 이름과 비밀번호 확인
2. PostgreSQL 사용자 비밀번호 재설정:
```sql
ALTER USER myuser WITH PASSWORD 'newpassword';
```

3. MySQL 사용자 비밀번호 재설정:
```sql
ALTER USER 'myuser'@'localhost' IDENTIFIED BY 'newpassword';
FLUSH PRIVILEGES;
```

### 문제 3: 데이터베이스가 존재하지 않음

**에러:**
```
django.db.utils.OperationalError: FATAL: database "myproject_db" does not exist
```

**해결 방법:**
데이터베이스를 생성하세요 (위의 "데이터베이스 생성" 섹션 참조)

### 문제 4: 권한 없음

**에러:**
```
django.db.utils.OperationalError: permission denied for database
```

**해결 방법:**
```sql
-- PostgreSQL
GRANT ALL PRIVILEGES ON DATABASE myproject_db TO myuser;

-- MySQL
GRANT ALL PRIVILEGES ON myproject_db.* TO 'myuser'@'localhost';
FLUSH PRIVILEGES;
```

### 문제 5: 포트 충돌

**에러:**
```
django.db.utils.OperationalError: could not connect to server
```

**해결 방법:**
1. 포트 번호 확인:
```bash
# PostgreSQL 기본 포트: 5432
# MySQL 기본 포트: 3306
```

2. 다른 포트 사용 중인지 확인:
```bash
# macOS/Linux
lsof -i :5432
lsof -i :3306
```

3. settings.py에서 포트 번호 확인

### 문제 6: 인코딩 문제 (한글 깨짐)

**해결 방법:**
1. 데이터베이스 생성 시 UTF-8 설정:
```sql
-- PostgreSQL
CREATE DATABASE myproject_db WITH ENCODING 'UTF8';

-- MySQL
CREATE DATABASE myproject_db 
    CHARACTER SET utf8mb4 
    COLLATE utf8mb4_unicode_ci;
```

2. settings.py에 옵션 추가:
```python
DATABASES = {
    'default': {
        'OPTIONS': {
            'charset': 'utf8mb4',
        },
    }
}
```

---

## 마무리

이 가이드를 통해 Django에서 다양한 데이터베이스를 연결하는 방법을 배웠습니다!

### 핵심 정리

1. **SQLite**: 개발 및 소규모 프로젝트에 적합
2. **PostgreSQL**: 프로덕션 환경에 적합
3. **MySQL**: 웹 애플리케이션에 많이 사용
4. **환경 변수**: 보안을 위한 필수 사항
5. **마이그레이션**: 데이터베이스 스키마 관리

### 선택 가이드

- **개발/학습**: SQLite (가장 간단)
- **소규모 프로젝트**: SQLite 또는 PostgreSQL
- **중대형 프로젝트**: PostgreSQL 또는 MySQL
- **기존 인프라 활용**: 해당 인프라의 데이터베이스

### 다음 단계

- 데이터베이스 백업 및 복원
- 성능 최적화
- 복제 및 클러스터링
- 모니터링 도구 사용

Happy Coding! 🚀

